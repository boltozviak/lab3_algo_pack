# from src.sortings.quick import insertion_sort


# def bucket_sort(arr: list[float], buckets: int | None = None) -> list[float]:
#     if len(arr) <= 1:
#         return arr

#     if buckets is None:
#         buckets = len(arr)
